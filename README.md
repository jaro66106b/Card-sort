
  # Kanban card sorting feature

  This is a code bundle for Kanban card sorting feature. The original project is available at https://www.figma.com/design/FFAHQTrAA7tfq18bRpMRYU/Kanban-card-sorting-feature.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  